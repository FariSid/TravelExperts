@model TravelExperts.Models.PackageViewModel

@{
    ViewData["Title"] = " | Package Details";
}

<h1>Package Details</h1>

@*<table class="table">*@
<table>
    <thead>
        <tr>
            <th scope="col">PackageId</th>
            <th scope="col">Pkg Name</th>
            <th scope="col">Pkg Start Date</th>
            <th scope="col">Pkg End Date</th>
            <th scope="col">Pkg Desc.</th>
            <th scope="col">Pkg Base Price</th>
            <th scope="col">Pkg Agency Commission</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>@Model.Package.PackageId</td>
            <td>@Model.Package.PkgName  </td>
            <td>@Model.Package.PkgStartDate</td>
            <td>@Model.Package.PkgEndDate</td>
            <td>@Model.Package.PkgDesc</td>
            <td>@Model.Package.PkgBasePrice</td>
            <td>@Model.Package.PkgAgencyCommission</td>
        </tr>
</table>

    <form asp-action="Add" asp-controller="Cart" method="post">
        <button type="submit" name="id" value="@Model.Package.PackageId"
                class="btn btn-primary">
            Add To Cart
        </button>
    </form>
